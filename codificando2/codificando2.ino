#include "funciones.h" // (creacion propia)buscar el archivo en el entorno de trabajo(dentro de la misma carpeta
//#include <funciones.h>//busque dentro de los archivos raiz del sftware
void setup() {
  // put your setup code here, to run once:
  configuracion();
}

void loop() {
  // put your main code here, to run repeatedly:
  espera();
  secuencia();
}
//.ino archivo principal con funciones principales
//.cpp tenemos la libreia de las funciones creadas
//.h solo las cabezeras de las condiciones (prototipos)